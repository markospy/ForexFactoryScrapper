# .env.example: copy to .env and adjust values as needed
# Host and port for Flask HTTP API
HOST=0.0.0.0
PORT=5000

# Debug mode: True or False
DEBUG=True

# Optional: custom path to an external .env file
# DOTENV_PATH=/path/to/.env
